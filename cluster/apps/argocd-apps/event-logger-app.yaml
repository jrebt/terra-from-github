apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: event-logger
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/jrebt/terra-from-github.git
    targetRevision: main
    path: cluster/apps/event-logger
  destination:
    server: https://kubernetes.default.svc
    namespace: event-logger
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
